# Let Spring throw NoHandlerFoundException for unknown routes
# so GlobalExceptionHandler can intercept it instead of /error
spring.mvc.throw-exception-if-no-handler-found=true
spring.web.resources.add-mappings=false
